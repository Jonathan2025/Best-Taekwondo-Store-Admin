import { HashLoader } from "react-spinners"

const UploadSpinner = () => {
  return (
    <HashLoader
        color="rgba(54, 215, 183, 1)"
        loading
        size={40}
        />
  )
}

export default UploadSpinner